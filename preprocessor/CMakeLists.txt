set(CMAKE_CXX_COMPILER g++-4.9)
cmake_minimum_required(VERSION 2.8)
project(preprocessor)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++14 -Ofast -fopenmp -g")
find_package(gflags REQUIRED)
find_package( OpenCV REQUIRED )
find_package(PCL REQUIRED)
include_directories(/home/erik/Projects/c++/globals)
include_directories(${PCL_INCLUDE_DIRS})
link_directories(${PCL_LIBRARY_DIRS})
add_definitions(${PCL_DEFINITIONS})
add_executable( preprocessor preprocessor.cpp )
target_link_libraries( preprocessor ${OpenCV_LIBS} ${PCL_LIBRARIES} gflags)